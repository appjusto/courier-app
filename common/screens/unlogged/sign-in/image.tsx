import colors from '@/common/styles/colors';
import { View } from 'react-native';

export const SignInImage = () => {
  return <View style={{ height: 256, width: '100%', backgroundColor: colors.neutral500 }}></View>;
};
